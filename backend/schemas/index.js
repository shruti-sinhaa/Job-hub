const jobResolver = require("../resolvers/jobResolver");
const userResolver = require("../resolvers/userResolver");
const applicationResolver = require("../resolvers/applicationResolver");

module.exports = [jobResolver, userResolver, applicationResolver];
